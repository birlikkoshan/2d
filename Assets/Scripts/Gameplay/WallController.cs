using UnityEngine;

/// <summary>
/// Marks a cell as an impassable wall.
/// BeamTracer stops the beam when it enters a cell with this component.
/// No logic required — presence of the component is sufficient.
/// </summary>
public class WallController : MonoBehaviour { }
